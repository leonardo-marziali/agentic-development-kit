{
  "name": "prettier-plugin-tests",
  "private": true,
  "description": "Runtime dependencies and test tooling for the prettier plugin's hook. Claude Code installs `dependencies` from package-lock.json when it caches the plugin; the hook itself still shells out to npx prettier.",
  "dependencies": {
    "@leonardo-marziali/ad-lfl-kit": "1.3.0"
  },
  "scripts": {
    "test": "node --test \"scripts/hooks/__tests__/*.test.js\"",
    "test:coverage": "mkdir -p coverage && node --test --experimental-test-coverage --test-coverage-exclude=\"**/__tests__/**\" --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=coverage/lcov.info \"scripts/hooks/__tests__/*.test.js\""
  }
}
